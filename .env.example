# Stellar Testnet Infrastructure Configuration
STELLAR_NETWORK=TESTNET
HORIZON_URL=https://stellar.org

# Your Freighter wallet's PUBLIC key only (never your browser secret keys)
STELLAR_PUBLIC_KEY=GXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX

# For server-side isolated signing of testnet demo packets
# Generate a FRESH testnet-only keypair — NEVER commit actual secret keys
STELLAR_DEMO_SECRET=SXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX

# Core Local Server Environment Ports
PORT=3001
ALLOWED_ORIGIN=http://localhost:5173
